# Example paragraph
paragraph = "Python is a powerful programming language. It is widely used in web development, data science, and automation. Python's simplicity makes it accessible to beginners."

# Split the paragraph into sentences using the period as the delimiter
sentences = paragraph.split(". ")

# Print each sentence
for sentence in sentences:
    print(sentence)
